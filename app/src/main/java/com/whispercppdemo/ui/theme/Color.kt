package com.whispercppdemo.ui.theme

import androidx.compose.ui.graphics.Color

// Palette extracted from the Kifak artwork (icon.png): white card on a gray page,
// cedar greens from lime to deep forest, and warm trunk brown.
val NeoBackground = Color(0xFFE3E4E6)   // page gray (artwork outer background)
val NeoSurface = Color(0xFFFCFCFD)      // card white (artwork card, softened a touch)
val NeoInset = Color(0xFFEDEEEF)        // pressed-in wells
val NeoShadowDark = Color(0xFFC3C5C8)   // soft gray shade for raised surfaces
val NeoShadowLight = Color(0xFFF6F7F8)  // subtle top-light for raised surfaces
val NeoText = Color(0xFF75421F)         // trunk shade — primary ink
val NeoTextSecondary = Color(0xFF87532A) // trunk brown — secondary text
val NeoTextFaint = Color(0x9987532A)     // trunk brown, softened — hints & captions

// Cedar greens, straight from the tree tiers
val NeoAccent = Color(0xFF2E7233)       // tier 3 deep cedar — primary accent
val NeoAccentSoft = Color(0xFF53932F)   // tier 2 mid green
val NeoAccentBright = Color(0xFF90B734) // tier 1 lime — idle/live flourish

// Trunk browns — the voice
val NeoBrown = Color(0xFF87532A)        // trunk
val NeoBrownDeep = Color(0xFF75421F)    // trunk shade
val NeoOnAccent = Color(0xFFFFFFFF)
